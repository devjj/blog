{
  "name": "blog",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node esbuild.config.js",
    "dev": "node esbuild.config.js",
    "esbuild": "node esbuild.config.js",
    "esbuild-dev": "node esbuild.config.js"
  },
  "devDependencies": {
    "esbuild": "^0.19.0"
  }
}